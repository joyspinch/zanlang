using System;

namespace Gui;

/// 图标集：语义名称映射到 "Segoe MDL2 Assets" 码点。
/// 绘制在画布上进行，因此使用名称作为
/// `canvas.DrawGlyph("close", x, y, size, color)`，或在控件内部使用
/// `canvas.DrawGlyphIn("close", bx, by, bw, bh, size, color)`。
class Icon {
    /// 将语义图标名称映射到 Segoe MDL2 Assets 码点。
    /// 未知名称返回 0（不绘制任何内容）。
    static int Codepoint(string name) {
        if (name == "close") { return 0xE8BB; }
        if (name == "minimize") { return 0xE921; }
        if (name == "maximize") { return 0xE922; }
        if (name == "restore") { return 0xE923; }
        if (name == "check") { return 0xE73E; }
        if (name == "plus") { return 0xE710; }
        if (name == "minus") { return 0xE738; }
        if (name == "arrow-left") { return 0xE76B; }
        if (name == "arrow-right") { return 0xE76C; }
        if (name == "arrow-up") { return 0xE70E; }
        if (name == "arrow-down") { return 0xE70D; }
        if (name == "search") { return 0xE721; }
        if (name == "settings") { return 0xE713; }
        if (name == "user") { return 0xE77B; }
        if (name == "bell") { return 0xEA8F; }
        if (name == "file") { return 0xE7C3; }
        if (name == "folder") { return 0xE8B7; }
        if (name == "home") { return 0xE80F; }
        if (name == "globe") { return 0xE774; }
        if (name == "trash") { return 0xE74D; }
        if (name == "archive") { return 0xE7B8; }
        if (name == "edit") { return 0xE70F; }
        if (name == "copy") { return 0xE8C8; }
        if (name == "info") { return 0xE946; }
        if (name == "warning") { return 0xE7BA; }
        if (name == "error") { return 0xEA39; }
        if (name == "success") { return 0xE930; }
        if (name == "star") { return 0xE735; }
        if (name == "star-empty") { return 0xE734; }
        if (name == "heart") { return 0xEB51; }
        if (name == "like") { return 0xE8E1; }
        if (name == "calendar") { return 0xE787; }
        if (name == "clock") { return 0xE823; }
        if (name == "image") { return 0xEB9F; }
        if (name == "download") { return 0xE896; }
        if (name == "upload") { return 0xE898; }
        if (name == "refresh") { return 0xE72C; }
        if (name == "more") { return 0xE712; }
        if (name == "menu") { return 0xE700; }
        if (name == "eye") { return 0xE7B3; }
        if (name == "lock") { return 0xE72E; }
        if (name == "mail") { return 0xE715; }
        if (name == "sun") { return 0xE706; }
        if (name == "moon") { return 0xE708; }
        if (name == "pin") { return 0xE718; }
        if (name == "unpin") { return 0xE77A; }
        if (name == "filter") { return 0xE71C; }
        if (name == "sort") { return 0xE8CB; }
        if (name == "chevron-down") { return 0xF101; }
        if (name == "chevron-up") { return 0xF103; }
        if (name == "chevron-right") { return 0xF102; }
        if (name == "chevron-left") { return 0xF104; }
        if (name == "table") { return 0xE80A; }
        if (name == "grid") { return 0xF0E2; }
        if (name == "play") { return 0xE768; }
        if (name == "pause") { return 0xE769; }
        if (name == "stop") { return 0xE71A; }
        if (name == "bug") { return 0xEBE8; }
        if (name == "code") { return 0xE943; }
        if (name == "save") { return 0xE74E; }
        if (name == "undo") { return 0xE7A7; }
        if (name == "redo") { return 0xE7A6; }
        if (name == "cut") { return 0xE8C6; }
        if (name == "paste") { return 0xE77F; }
        if (name == "select-all") { return 0xE8B3; }
        if (name == "hand") { return 0xE7C9; }
        if (name == "align-center") { return 0xE8E3; }
        if (name == "align-right") { return 0xE8E2; }
        if (name == "align-top") { return 0xF210; }
        if (name == "align-middle") { return 0xF211; }
        if (name == "align-bottom") { return 0xF212; }
        if (name == "font") { return 0xE8D2; }
        if (name == "color") { return 0xE790; }
        if (name == "layout") { return 0xE8A1; }
        if (name == "align-left") { return 0xE8E4; }
        if (name == "chart-line") { return 0xE9D2; }
        if (name == "chart-bar") { return 0xE9D3; }
        return 0;
    }

    /// 语义名到 SVG 图标名（Tabler 原名）的差异映射：只列与 Tabler
    /// 同名不同形的那些；返回 "" 表示同名直用。DrawGlyph/DrawGlyphIn
    /// 优先走 IconSvg，这里查不到的再回退 IconVector 手绘。
    /// 目标名均核对过存在于 IconSvgData；align-top/middle/bottom 上游
    /// 没有对应形，保持手绘。
    static string SvgName(string name) {
        if (name == "close") { return "x"; }
        if (name == "star") { return "star-filled"; }
        if (name == "star-empty") { return "star"; }
        if (name == "like") { return "thumb-up"; }
        if (name == "more") { return "dots"; }
        if (name == "save") { return "device-floppy"; }
        if (name == "undo") { return "arrow-back-up"; }
        if (name == "redo") { return "arrow-forward-up"; }
        if (name == "cut") { return "scissors"; }
        if (name == "paste") { return "clipboard"; }
        if (name == "image") { return "photo"; }
        if (name == "globe") { return "world"; }
        if (name == "color") { return "palette"; }
        if (name == "sort") { return "sort-descending"; }
        if (name == "font") { return "typography"; }
        if (name == "grid") { return "grid-dots"; }
        if (name == "layout") { return "layout-dashboard"; }
        if (name == "edit") { return "pencil"; }
        if (name == "error") { return "circle-x"; }
        if (name == "success") { return "circle-check"; }
        if (name == "warning") { return "alert-triangle"; }
        if (name == "info") { return "info-circle"; }
        if (name == "hand") { return "hand-stop"; }
        // 窗口按钮语义要的是窗口铬件的形状（一条线/一个方框/两叠
        // 方框），而不是 Tabler 的 maximize/minimize（对角箭头）。
        if (name == "minimize") { return "minus"; }
        if (name == "maximize") { return "square"; }
        if (name == "restore") { return "copy"; }
        if (name == "unpin") { return "pinned-off"; }
        if (name == "play") { return "player-play"; }
        if (name == "pause") { return "player-pause"; }
        if (name == "stop") { return "player-stop"; }
        return "";
    }

    /// Codepoint 认识的所有图标名，按显示顺序。让调用方（如
    /// 组件画廊）无需硬编码即可枚举完整图标集。
    static List<string> Names() {
        List<string> n = new List<string>{
            "close", "minimize", "maximize", "restore",
            "check", "plus", "minus",
            "arrow-left", "arrow-right", "arrow-up", "arrow-down",
            "search", "settings", "user", "bell",
            "file", "folder", "home", "globe", "trash", "archive", "edit",
            "copy", "info", "warning", "error", "success",
            "star", "star-empty", "heart", "like",
            "calendar", "clock", "image", "download",
            "upload", "refresh", "more", "menu", "eye",
            "lock", "mail", "sun", "moon", "pin",
            "unpin", "filter", "sort", "chevron-down",
            "chevron-up", "chevron-right", "chevron-left", "table", "grid", "play",
            "pause", "stop", "bug", "code", "save",
            "undo", "redo", "cut", "paste",
            "select-all", "font", "color", "layout",
            "align-left", "chart-line", "chart-bar" };
        return n;
    }
}
