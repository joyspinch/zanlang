// 由 scripts/generate_jd_sdk.py 从 Request/DmpNewTagInfoSkusRequest.cs 生成 —— 请勿编辑。

using System;
using System.Json;
using Sdk.Jd;
using Sdk.Jd.Domain;

namespace Sdk.Jd.Api.Dmp;

/// <summary><c>jingdong.dmp.new.tag.info.skus</c> 的响应。</summary>
class DmpNewTagInfoSkusResponse {
    public JsonCommonResponse data;
    /// <summary>用于诊断的完整 JOS 响应信封。</summary>
    public string Raw;
}

/// <summary><c>jingdong.dmp.new.tag.info.skus</c> 的请求。</summary>
class DmpNewTagInfoSkusRequest {
    JdRequest req;

    public DmpNewTagInfoSkusRequest() {
        this.req = new JdRequest("jingdong.dmp.new.tag.info.skus");
    }

    /// <summary>设置 <c>accessPin</c> 参数。</summary>
    DmpNewTagInfoSkusRequest AccessPin(string accessPin) {
        this.req.Set("accessPin", accessPin);
        return this;
    }

    /// <summary>设置 <c>authType</c> 参数。</summary>
    DmpNewTagInfoSkusRequest AuthType(string authType) {
        this.req.Set("authType", authType);
        return this;
    }

    /// <summary>设置 <c>skuIds</c> 参数。</summary>
    DmpNewTagInfoSkusRequest SkuIds(long skuIds) {
        this.req.SetLong("skuIds", skuIds);
        return this;
    }

    /// <summary>设置 <c>type</c> 参数。</summary>
    DmpNewTagInfoSkusRequest Type(int type) {
        this.req.SetInt("type", type);
        return this;
    }

    /// <summary>底层协议请求。</summary>
    JdRequest Raw() { return this.req; }
}
