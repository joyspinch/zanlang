// 由 scripts/generate_jd_sdk.py 从 Request/ImageReadFindImagesByColorRequest.cs 生成 —— 请勿编辑。

using System;
using System.Json;
using Sdk.Jd;
using Sdk.Jd.Domain;

namespace Sdk.Jd.Api.Image;

/// <summary><c>jingdong.image.read.findImagesByColor</c> 的响应。</summary>
class ImageReadFindImagesByColorResponse {
    public List<string> images;
    /// <summary>用于诊断的完整 JOS 响应信封。</summary>
    public string Raw;
}

/// <summary><c>jingdong.image.read.findImagesByColor</c> 的请求。</summary>
class ImageReadFindImagesByColorRequest {
    JdRequest req;

    public ImageReadFindImagesByColorRequest() {
        this.req = new JdRequest("jingdong.image.read.findImagesByColor");
    }

    /// <summary>设置 <c>wareId</c> 参数。</summary>
    ImageReadFindImagesByColorRequest WareId(long wareId) {
        this.req.SetLong("wareId", wareId);
        return this;
    }

    /// <summary>设置 <c>colorId</c> 参数。</summary>
    ImageReadFindImagesByColorRequest ColorId(string colorId) {
        this.req.Set("colorId", colorId);
        return this;
    }

    /// <summary>底层协议请求。</summary>
    JdRequest Raw() { return this.req; }
}
