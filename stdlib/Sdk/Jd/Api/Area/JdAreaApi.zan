// 由 scripts/generate_jd_sdk.py 从 Request/area module 生成 —— 请勿编辑。

using System;
using System.Json;
using Sdk.Jd;

namespace Sdk.Jd.Api.Area;

/// <summary>jingdong.area.* 的强类型客户端。</summary>
class JdAreaApi {
    JdClient client;

    public JdAreaApi(JdClient client) { this.client = client; }

    /// <summary>执行 <c>jingdong.area.province.get</c>。</summary>
    async AreaProvinceGetResponse ProvinceGetAsync(AreaProvinceGetRequest request) {
        JdResponse raw = await this.client.ExecuteAsync(request.Raw());
        AreaProvinceGetResponse result = Json.Deserialize<AreaProvinceGetResponse>(raw.Data);
        result.Raw = raw.Raw;
        return result;
    }

}
