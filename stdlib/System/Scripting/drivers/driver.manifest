# Native [DllImport]/Interop.Load libraries owned by this module (one -l
# basename per line). Blank lines and '#' comments are ignored. zanc discovers
# this file to bundle the driver on --publish; the payload copied next to the
# executable is listed in <name>.bundle beside the target subdirectory
# (lua.bundle). See src/compiler/main.c (zan_discover_drivers).
lua
