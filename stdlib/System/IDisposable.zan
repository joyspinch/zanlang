// IDisposable -- the standard disposal contract used by `using` statements.
namespace System;

/// <summary>
/// 标准释放契约：持有原生资源的对象实现它，`using` 语句
/// 在作用域结束时调用 <see cref="Dispose"/>。
/// </summary>
interface IDisposable {
    /// 释放对象持有的资源。
    void Dispose();
}
