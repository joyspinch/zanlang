using System;
using Gui;
using Gui.Widget;

/* {{NAME}} 业务逻辑：NavPanel 为左侧导航栏，ContentPanel 为页面区域，标题显示在 PageTitle。 */
partial class {{NAME}} {
    static void OnLoad(Form form) {
        DashboardNav.OnClick(() => { Show("仪表盘"); });
        DevicesNav.OnClick(() => { Show("设备"); });
        ReportsNav.OnClick(() => { Show("报表"); });
        SettingsNav.OnClick(() => { Show("设置"); });
    }

    /* 切换页面标题；可基于此方法扩展自定义页面切换。 */
    static void Show(string page) {
        PageTitle.Text = page;
        MainStatusBar.SetText(0, page);
    }
}
